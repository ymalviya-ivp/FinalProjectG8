import React from 'react';

const Positions = () => {
    return (
        <div className="panel">
            <h3>Current Positions</h3>
            <p className="placeholder-text">Positions data will appear here once the API is connected.</p>
            {/* Future AG-Grid or enterprise table here */}
        </div>
    );
};

export default Positions;